name: Release Drafter

on:
  push:
    tags:
      - '*'  # Or use workflow_dispatch for manual

jobs:
  draft_release:
    runs-on: ubuntu-latest
    steps:
      - uses: release-drafter/release-drafter@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
