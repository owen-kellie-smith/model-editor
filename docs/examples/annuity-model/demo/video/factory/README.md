# factory

1. Get clips (SHIFT-CTRL-ALT-R in Ubuntu) and list them in order at the top of bash_video.
2. Run `./bash_video`
3. Run `./bash_subtitle` and check the output.
4. Type over final_output.srt and rerun ./bash_subtitle.

