# factory

1. Make clips (SHIFT-CTRL-ALT-R in Ubuntu).
2. Add clip filenames and subtitles at the top of `bash_video` in the list `files_in_order_with_captions`.
3. Run `./bash_video` which outputs `../demo.gif` and `../overview_final.mp4`.

